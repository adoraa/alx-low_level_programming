#include "main.h"

/**
 * _islower - checks for lowercase characters
 * @c: first parameter
 * Return: returns 1 or 0 depending on the value of _islower
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
